<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mdtest">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".TestActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".TabInAppBarActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".FABInCoordinateLayoutActivity" />
        <activity android:name=".CollapsingToolbarInAppBarActivity" />
        <activity android:name=".SwipeDismissActivity" />
        <activity android:name=".SwipeDeleteActivity" />
        <activity android:name=".TestConstraintLayoutActivity" />
        <activity android:name=".NavigationViewActivity" />
        <activity android:name="com.example.view.TestPathActivity" />
        <activity
            android:name="com.example.html.WebVideoActivity"
            android:configChanges="orientation|screenSize"
            android:hardwareAccelerated="true"
            android:screenOrientation="portrait" />

        <activity android:name="com.example.nestedscroll.TestOffsetActivity" />
    </application>

</manifest>